[pytest]
minversion = 6.0
addopts = -ra -q --cov=log_analyzer --cov-report=term-missing
testpaths =
    tests
markers =
    integration: marks tests as integration tests (require external services like MQTT broker) 